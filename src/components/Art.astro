---
interface Props {
    title: string;
    class: string;
    type: "christian" | "arabian";
}
const { title, class: bg, type } = Astro.props;
---

<article
    class={`${bg} rounded-xl h-80 w-[356px] text-center flex  flex-col items-center  text-wrap`}
>
    <h3
        class={`text-2xl ${type === "christian" ? "font-old" : "font-arabian"}`}
    >
        {title}
    </h3>
    <p><slot /></p>
</article>
