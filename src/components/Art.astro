---
interface Props {
    title: string;
    class: string;
}
const { title, class: bg } = Astro.props;
---

<article class={`${bg} rounded-xl p-4 w-36 text-center cursor-pointer`}>
    <h3 class="text-2xl font-old">{title}</h3>
    <p>Saber más</p>
</article>
